spring:
  application:
    name: restaurant-crud
  datasource:
    url: jdbc:mysql://localhost:3308/plaza-crud
    username: root
    password: laura
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8090
logging:
  level:
    org.springframework.web: DEBUG
debug: true
jwt:
  secret: clavesecreta123lauramoyanoesmuylinda

